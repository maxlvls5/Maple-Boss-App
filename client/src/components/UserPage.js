import React from "react";
import GuideCard from "./GuideCard";

function UserPage({ user }) {
  //   const userGuides =
  //     user &&
  //     user.reviews.map((r) => (
  //       <GuideCard key={r.id} r={r} user={user} setUser={setUser} />
  //     ));

  return (
    <div>
      <h1 align="center">user's Page</h1>
    </div>
  );
}

export default UserPage;
